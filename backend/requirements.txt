# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12
aiofiles==24.1.0

# --- Validation & config ---
pydantic==2.9.2
pydantic-settings==2.5.2
filetype==1.2.0

# --- Computer vision ---
# GPU (CUDA): after `pip install -r requirements.txt`, install a CUDA build of PyTorch
# from https://pytorch.org/get-started/locally/ (see docs/DEPLOYMENT.md). Otherwise
# Ultralytics falls back to CPU-only Torch wheels.
# Python 3.13: NumPy 1.26 has no Windows wheel → pip builds from source (needs GCC ≥ 8.4).
# ultralytics 8.3.0 declares numpy<2, which forces that backtrack. Pin NumPy 2.x and use
# a newer ultralytics that supports it (wheels only, no local compiler).
numpy>=2.1.0,<3
ultralytics>=8.3.128,<9
opencv-python-headless>=4.10.0.84,<5
supervision==0.23.0

# --- Reporting ---
pandas>=2.2.3,<3
openpyxl==3.1.5

# --- Logging ---
loguru==0.7.2
